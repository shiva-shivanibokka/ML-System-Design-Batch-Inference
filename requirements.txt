# =============================================================================
# Full requirements — for local development
# =============================================================================

# Core ML
lightgbm==4.3.0
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2

# PySpark
pyspark==3.5.1
pyarrow==16.1.0        # Spark ↔ pandas bridge + Parquet I/O

# Data generation
Faker==25.2.0

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
asyncpg==0.29.0        # async PostgreSQL driver for FastAPI

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1

# Monitoring
scipy==1.13.0          # PSI / KS test
matplotlib==3.9.0      # benchmark chart

# Dashboard is a separate Next.js app (dashboard/) — no Python UI deps here.

# Airflow (installed separately in Dockerfile.airflow)
# apache-airflow==2.9.3

# Configuration
PyYAML==6.0.1
python-dotenv==1.0.1

# Utilities
tqdm==4.66.4
rich==13.7.1           # pretty terminal output for training/benchmark scripts
