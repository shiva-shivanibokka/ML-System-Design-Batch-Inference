# URL of the FastAPI serving layer (the API's Vercel deployment).
# Local dev against `uvicorn serving.app:app`:
NEXT_PUBLIC_API_URL=http://localhost:8000
# Production example:
# NEXT_PUBLIC_API_URL=https://batch-inference-api.vercel.app
